@model IList<FICTFeed.MVC.Models.PageViews.User.EditUserPageView>
@{
    ViewBag.Title = "GetUsers";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Пользователи</h2>

@if (Model != null)
{
    foreach (var item in Model)
    {
        <div class="custom-card small z-depth-2">
            <div class="card-content">
                <span class="card-title grey-text">@Html.DisplayFor(m => item.User.Name)</span>
                <p>
                    <ul>
                        <li>Mail: @Html.DisplayFor(m => item.User.Mail)</li>
                        <li>Current role: @Html.DisplayFor(m => item.User.Role)</li>
                    </ul>
                    
                    <div class="default-input-width">
                        @Html.EditorFor(m => item.User.Role)
                    </div>
                </p>
            </div>
            <div class="card-action">
                <a href="#" class="btn-flat grey-text">Save</a>
            </div>
        </div>
        //@Html.RouteLink("Edit", "Edit", new { id = item.User.Id })
    }
}
